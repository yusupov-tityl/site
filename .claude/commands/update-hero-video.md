# Update Hero Video

Find the newest video file in the project root directory (mp4, webm, mov) that is NOT inside a subfolder, then copy it to `artifacts/itityl/public/hero-bg.mp4`.

Steps:
1. Search for video files (*.mp4, *.webm, *.mov) in the project root `/Users/anton/Desktop/ittool/` (top level only, not recursive)
2. Pick the most recently modified one
3. Copy it to `artifacts/itityl/public/hero-bg.mp4`
4. Confirm the update by checking the file size
5. Reload the preview if a dev server is running
